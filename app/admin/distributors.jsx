import React from 'react';
import { ActivityIndicator, Modal, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import AdminShell from '../../components/AdminShell';
import { TableSkeleton } from '../../components/AdminSkeleton';
import { useAdminTheme } from '../../components/AdminTheme';
import { getBranches, getDistributors, updateDistributor } from '../../services/branchManagement';
import { ADMIN_CACHE_KEYS, useAdminData } from '../../services/adminDataCache';

const labels = { pending: 'Pending', active: 'Active', inactive: 'Inactive', rejected: 'Rejected', approved: 'Active' };
const branchLabel = (item) => item.branchName || (item.branchId ? item.branchId : 'Branch assignment required');

export default function DistributorManagement() {
  const { colors } = useAdminTheme(); const styles = createStyles(colors);
  const distributorState = useAdminData(ADMIN_CACHE_KEYS.distributors, getDistributors);
  const branchState = useAdminData(ADMIN_CACHE_KEYS.branches, getBranches);
  const items = distributorState.data || []; const activeBranches = (branchState.data || []).filter((branch) => branch.status === 'active');
  const [filter, setFilter] = React.useState('pending'); const [query, setQuery] = React.useState(''); const [debouncedQuery, setDebouncedQuery] = React.useState(''); const [actionError, setActionError] = React.useState(''); const [busy, setBusy] = React.useState(''); const [approval, setApproval] = React.useState(null); const [branchQuery, setBranchQuery] = React.useState('');
  React.useEffect(() => { const timer = setTimeout(() => setDebouncedQuery(query.trim().toLowerCase()), 200); return () => clearTimeout(timer); }, [query]);
  const act = async (uid, action, options = {}) => {
    setBusy(uid + action); setActionError('');
    try { await updateDistributor(uid, action, options); await Promise.all([distributorState.refresh({ force: true }), branchState.refresh({ force: false })]); if (action === 'approve') setApproval(null); }
    catch (caught) { setActionError(caught.message || 'Unable to update this distributor.'); }
    finally { setBusy(''); }
  };
  const openApproval = (item) => { setApproval({ item, branchId: activeBranches.some((branch) => branch.id === item.requestedBranchId) ? item.requestedBranchId : '' }); setBranchQuery(''); };
  const visible = items.filter((item) =>
    (item.approvalStatus === filter || (filter === 'active' && item.approvalStatus === 'approved')) &&
    `${item.fullName} ${item.username} ${item.email} ${item.branchName} ${item.requestedBranchName}`.toLowerCase().includes(debouncedQuery)
  );
  const loadError = actionError || distributorState.error || branchState.error;
  const branchChoices = activeBranches.filter((branch) => `${branch.name} ${branch.code || ''}`.toLowerCase().includes(branchQuery.trim().toLowerCase()));

  return <AdminShell title="Distributor Management" subtitle="Review applications and manage branch-assigned Distributor accounts.">
    <View style={styles.card}>
      <View style={styles.tabs}>{['pending', 'active', 'inactive', 'rejected'].map((value) => <Pressable key={value} onPress={() => setFilter(value)} style={[styles.tab, filter === value && styles.tabActive]}><Text style={[styles.tabText, filter === value && styles.tabTextActive]}>{labels[value]} ({items.filter((item) => item.approvalStatus === value || (value === 'active' && item.approvalStatus === 'approved')).length})</Text></Pressable>)}</View>
      <TextInput value={query} onChangeText={setQuery} placeholder="Search name, username, email, or branch" placeholderTextColor={colors.textSecondary} style={styles.search} />
      <Text style={styles.count}>{distributorState.refreshing ? 'Refreshing distributors…' : `${visible.length} distributor${visible.length === 1 ? '' : 's'}`}</Text>
      {!!loadError && <View style={styles.errorBox}><Text style={styles.error}>{loadError}</Text><Pressable onPress={() => distributorState.refresh({ force: true })}><Text style={styles.retry}>Retry</Text></Pressable></View>}
      {distributorState.loading && !distributorState.data ? <TableSkeleton rows={4} /> : visible.length === 0 ? <Text style={styles.empty}>No {labels[filter].toLowerCase()} distributors.</Text> : visible.map((item) => <View key={item.uid} style={styles.row}><View style={styles.copy}><Text style={styles.name}>{item.fullName || item.username}</Text><Text style={styles.meta}>{item.username} · {item.email}</Text><Text style={styles.meta}>{item.phone || 'No phone'} {item.barangay ? `· ${item.barangay}` : ''}</Text>{item.approvalStatus === 'pending' ? <Text style={styles.branch}>Requested branch: {item.requestedBranchName || 'No branch selected'}</Text> : <Text style={styles.branch}>Assigned branch: {branchLabel(item)}</Text>}<Text style={styles.badge}>{labels[item.approvalStatus] || item.approvalStatus}</Text></View><View style={styles.actions}>{item.approvalStatus === 'pending' && <><Pressable disabled={!!busy} style={styles.approve} onPress={() => openApproval(item)}><Text style={styles.buttonText}>Review & approve</Text></Pressable><Pressable disabled={!!busy} style={styles.reject} onPress={() => act(item.uid, 'reject')}><Text style={styles.rejectText}>Reject</Text></Pressable></>}{item.approvalStatus === 'active' && <Pressable disabled={!!busy} style={styles.reject} onPress={() => act(item.uid, 'deactivate')}><Text style={styles.rejectText}>Deactivate</Text></Pressable>}{item.approvalStatus === 'inactive' && <Pressable disabled={!!busy} style={styles.approve} onPress={() => act(item.uid, 'reactivate')}><Text style={styles.buttonText}>Reactivate</Text></Pressable>}{busy.startsWith(item.uid) && <ActivityIndicator color={colors.primary} />}</View></View>)}
    </View>
    <Modal visible={!!approval} transparent animationType="fade" onRequestClose={() => setApproval(null)}><View style={styles.modalBackdrop}><View style={[styles.modal, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={styles.modalTitle}>Assign branch and approve</Text><Text style={styles.modalCopy}>Approve {approval?.item?.fullName || 'this Distributor'} for an active BlueTap branch. The requested branch is selected when it remains active.</Text><TextInput value={branchQuery} onChangeText={setBranchQuery} placeholder="Search active branches" placeholderTextColor={colors.textSecondary} style={styles.search} /><ScrollView style={styles.branchList} nestedScrollEnabled>{branchChoices.map((branch) => <Pressable key={branch.id} onPress={() => setApproval((current) => ({ ...current, branchId: branch.id }))} style={[styles.branchOption, approval?.branchId === branch.id && { borderColor: colors.primary, backgroundColor: colors.primarySoft }]}><Text style={styles.branchName}>{branch.name}</Text><Text style={styles.branchCode}>{branch.code || branch.id}</Text></Pressable>)}{!branchChoices.length && <Text style={styles.empty}>No active branches match.</Text>}</ScrollView><View style={styles.modalActions}><Pressable onPress={() => setApproval(null)} style={styles.cancel}><Text style={styles.cancelText}>Cancel</Text></Pressable><Pressable disabled={!approval?.branchId || !!busy} onPress={() => act(approval.item.uid, 'approve', { branchId: approval.branchId })} style={[styles.approve, (!approval?.branchId || !!busy) && styles.disabled]}><Text style={styles.buttonText}>{busy ? 'Approving…' : 'Confirm approval'}</Text></Pressable></View></View></View></Modal>
  </AdminShell>;
}

const createStyles = (colors) => StyleSheet.create({
  card: { backgroundColor: colors.surface, borderRadius: 16, padding: 20, borderWidth: 1, borderColor: colors.border }, tabs: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginBottom: 16 }, tab: { paddingHorizontal: 12, paddingVertical: 9, borderRadius: 9, backgroundColor: colors.surfaceAlt }, tabActive: { backgroundColor: colors.primary }, tabText: { color: colors.textSecondary, fontWeight: '700' }, tabTextActive: { color: '#FFF' }, search: { borderWidth: 1, borderColor: colors.inputBorder, borderRadius: 10, padding: 12, color: colors.textPrimary, backgroundColor: colors.input, outlineStyle: 'none' }, count: { marginVertical: 14, color: colors.textSecondary }, row: { paddingVertical: 15, borderTopWidth: 1, borderTopColor: colors.border, flexDirection: 'row', gap: 12 }, copy: { flex: 1 }, name: { fontWeight: '800', color: colors.textPrimary, fontSize: 16 }, meta: { color: colors.textSecondary, marginTop: 3, fontSize: 12 }, branch: { color: colors.textPrimary, marginTop: 7, fontSize: 12, fontWeight: '700' }, badge: { color: colors.primary, fontWeight: '800', marginTop: 7, fontSize: 12 }, actions: { gap: 8, alignItems: 'flex-end' }, approve: { backgroundColor: colors.success, borderRadius: 8, paddingHorizontal: 12, paddingVertical: 9 }, reject: { borderWidth: 1, borderColor: colors.danger, borderRadius: 8, paddingHorizontal: 12, paddingVertical: 9 }, buttonText: { color: '#FFF', fontWeight: '800' }, rejectText: { color: colors.danger, fontWeight: '800' }, empty: { color: colors.textSecondary, textAlign: 'center', padding: 20 }, errorBox: { borderWidth: 1, borderColor: colors.danger, backgroundColor: colors.dangerSoft, borderRadius: 10, padding: 12, marginBottom: 10 }, error: { color: colors.danger, fontWeight: '700' }, retry: { color: colors.primary, fontWeight: '900', marginTop: 7 }, modalBackdrop: { flex: 1, backgroundColor: colors.overlay, padding: 20, justifyContent: 'center', alignItems: 'center' }, modal: { width: '100%', maxWidth: 480, maxHeight: '88%', borderRadius: 16, borderWidth: 1, padding: 20 }, modalTitle: { color: colors.textPrimary, fontSize: 19, fontWeight: '900' }, modalCopy: { color: colors.textSecondary, fontSize: 13, lineHeight: 19, marginTop: 7, marginBottom: 14 }, branchList: { maxHeight: 250, marginTop: 12 }, branchOption: { borderWidth: 1, borderColor: colors.border, borderRadius: 10, padding: 12, marginBottom: 8 }, branchName: { color: colors.textPrimary, fontWeight: '800' }, branchCode: { color: colors.textSecondary, fontSize: 12, marginTop: 3 }, modalActions: { flexDirection: 'row', justifyContent: 'flex-end', gap: 10, marginTop: 12 }, cancel: { borderWidth: 1, borderColor: colors.inputBorder, borderRadius: 8, paddingHorizontal: 12, paddingVertical: 9 }, cancelText: { color: colors.textPrimary, fontWeight: '800' }, disabled: { opacity: .55 },
});
